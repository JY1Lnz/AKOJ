#include<bits/stdc++.h>
using namespace std;

int main()
{
    int k;
    while(cin>>k)
    {
        if (k == 0)
            break;
        string s;
        int len = s.length()/k;
        for ()
    }

    return 0;
}
